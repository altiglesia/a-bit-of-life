import React from "react";
import QuestionItem from "./QuestionItem";

function Quiz() {
    // this will be where all the fetch to your db.json file for quiz questions will be
    // then you will map over each question, and render a component for them
    return (
        <section>
            <ul></ul>
        </section>
    )
};

export default Quiz;